services:
  server:
    build: ./server
    ports:
      - "${PORT:-8080}:8080"
    environment:
      - PORT=8080
      - API_KEY=${API_KEY:-secretpassword}
    restart: unless-stopped
    # Optional: Persist data if we were writing to a file (not implemented in this memory-only version)
    # volumes:
    #   - ./data:/app/data
